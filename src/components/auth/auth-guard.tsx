// This component has been temporarily disabled and backed up to /src/auth-backup/auth-guard.tsx.bak
'use client';

// The AuthGuard has been temporarily disabled.
export function AuthGuard({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
