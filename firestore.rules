rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Authentication is paused. Allow all reads and writes for now.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
